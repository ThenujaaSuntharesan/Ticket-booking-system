package com.realtimetickets.realtimetickets;

// Import required Spring Boot annotations and classes
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication  // Indicates this is a Spring Boot application and enables auto-configuration, component scanning, and configuration support
public class RealtimeticketsApplication {

	public static void main(String[] args) {
		// Launch the Spring Boot application by running the RealtimeticketsApplication clas
		SpringApplication.run(RealtimeticketsApplication.class, args);

	}

}
